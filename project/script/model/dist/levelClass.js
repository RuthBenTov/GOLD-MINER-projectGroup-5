var Level = /** @class */ (function () {
    function Level(NumLevel, map, targetScore, timeToEnd) {
        this.NumLevel = NumLevel;
        this.map = map;
        this.targetScore = targetScore;
        this.timeToEnd = timeToEnd;
    }
    return Level;
}());
